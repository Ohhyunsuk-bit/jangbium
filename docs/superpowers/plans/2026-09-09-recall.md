# 장비움 추적검사 계산기 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 대장내시경에서 용종을 절제한 뒤 "다음 검사는 언제인가"를 2022년 한국 지침(Table 4)
기준으로 계산하는 정적 페이지 두 장(의사 입력 `recall.html`, 환자 열람 `recall-plan.html`)을
장비움에 추가한다.

**Architecture:** 장비움과 동일한 무빌드 정적 사이트 패턴을 그대로 따른다. 순수 계산은
`js/recall.js`(DOM 없음, node --test로 커버)에 두고, 화면은 `js/recall-app.js`(DOM 글루,
브라우저로만 검증)가 담당한다. 결과는 서버·저장 없이 URL 쿼리 파라미터로만 재현되며, 환자
페이지는 그 URL을 벤더링한 QR 라이브러리로 SVG QR 코드를 그린다. `js/app.js`가 갖고 있던
`esc`/`toast`/`share`를 `js/ui-utils.js`로 뽑아 두 페이지군이 공유한다.

**Tech Stack:** 순수 ES 모듈(`type="module"`), Node 24 `node --test`, RFC 5545 ICS(기존
`js/ics.js` 재사용), MIT 라이선스 QR 인코더(kazuhikoarase/qrcode-generator) 벤더 카피.

**Spec:** [docs/superpowers/specs/2026-09-09-recall-design.md](../specs/2026-09-09-recall-design.md)

## Global Constraints

- 색상은 `css/style.css`에 이미 정의된 CSS 변수(`--accent`, `--ink` 등)만 쓴다. 새 색을
  하드코딩하지 않는다.
- 서버·계정·저장소가 없다. 모든 상태는 URL 쿼리 파라미터로만 오간다(`js/schedule.js`의
  `plan.html?p=&t=` 패턴과 동일 철학).
- 실명을 어디에도 넣지 않는다. 푸터 문구는 기존 페이지들과 동일하게 "정보 제공 목적…
  소화기내과 전문의 감수"를 쓴다.
- 추적 간격 표는 스펙 문서의 Table 4를 그대로 따른다 — 값을 코드에 넣을 때 스펙의 표와
  한 글자도 다르지 않아야 한다(간격 개월수·경계값 포함).
- 새 파일을 만들 때 기존 파일의 코딩 스타일(주석 위치, 2칸 들여쓰기, 함수형 내보내기)을
  따른다.
- 순수 로직(`js/recall.js`)은 반드시 `node --test`로 커버한다. DOM 글루(`js/recall-app.js`)는
  기존 `js/app.js`와 마찬가지로 node 테스트를 만들지 않고 브라우저로 직접 검증한다.
- 새로 만드는 페이지(`recall.html`, `recall-plan.html`)는 배포 파일 목록
  (`tools/deploy-pages.sh`와 `.github/workflows/deploy.yml` 양쪽)에 반드시 추가한다 —
  빠뜨리면 로컬에서는 되는데 실제 사이트에는 404가 뜬다.

---

## Task 1: 공용 UI 도우미 분리 (`js/ui-utils.js`)

`js/app.js`에 있던 `esc`/`toast`/`share`를 새 모듈로 뽑아 `recall-app.js`(Task 6-7)와
공유한다. 기존 동작은 하나도 바뀌지 않아야 한다.

**Files:**
- Create: `js/ui-utils.js`
- Modify: `js/app.js` (전체 재작성)
- Test: 기존 `npm test`(37개)가 그대로 통과하는지로 검증 (이 모듈은 DOM 전용이라 node
  테스트 대상이 아님 — 기존 `js/app.js` 관행과 동일)

**Interfaces:**
- Produces: `esc(s): string`, `toast(msg: string): void`, `share({ title, url }): Promise<void>`,
  `copyText(text: string, successMsg?: string): Promise<void>` — Task 6-7이 그대로 가져다 쓴다.

- [ ] **Step 1: `js/ui-utils.js` 작성**

```js
// 공용 DOM 도우미. app.js와 recall-app.js가 함께 쓴다.
export function esc(s) {
  return String(s).replace(/[&<>"']/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
}

let toastTimer;
export function toast(msg) {
  const el = document.getElementById('toast');
  if (!el) return;
  el.textContent = msg;
  el.classList.add('is-on');
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => el.classList.remove('is-on'), 2200);
}

export async function share({ title, url }) {
  if (navigator.share) {
    try {
      await navigator.share({ title, text: title, url });
      return;
    } catch (err) {
      if (err && err.name === 'AbortError') return;
    }
  }
  try {
    await navigator.clipboard.writeText(url);
    toast('링크를 복사했습니다');
  } catch {
    toast('주소창의 링크를 복사해 주세요');
  }
}

export async function copyText(text, successMsg = '복사했습니다') {
  try {
    await navigator.clipboard.writeText(text);
    toast(successMsg);
  } catch {
    toast('복사에 실패했습니다. 직접 선택해 복사해 주세요.');
  }
}
```

- [ ] **Step 2: `js/app.js`를 아래 내용으로 전체 교체**

기존 파일에서 로컬 `esc`/`toast`/`share` 정의를 지우고 `js/ui-utils.js`에서 가져오도록
바꾼 것 외에는 동작이 동일하다. `share()` 호출부가 `{ meta }`를 넘기던 것에서
`{ title, url }`을 직접 조립해 넘기는 것으로 바뀐다(어떤 화면에서 부르든 재사용 가능하게).

```js
import { PRODUCTS } from './protocols.js';
import { computeSchedule, parseParam, toParam, formatTime, formatDateKo, addDays, startOfDay } from './schedule.js';
import { buildIcs } from './ics.js';
import { esc, toast, share } from './ui-utils.js';

document.addEventListener('DOMContentLoaded', () => {
  const page = document.body.dataset.page;
  if (page === 'product') initProductForm();
  if (page === 'plan') initPlanPage();
});

function initProductForm() {
  const productId = document.body.dataset.product;
  if (!PRODUCTS[productId]) return;
  const form = document.getElementById('exam-form');
  const dateEl = form.elements.date;
  const timeEl = form.elements.time;

  const today = new Date();
  dateEl.min = toParam(today).slice(0, 10);
  if (!dateEl.value) dateEl.value = toParam(addDays(today, 1)).slice(0, 10);
  if (!timeEl.value) timeEl.value = '09:00';

  form.addEventListener('submit', (ev) => {
    ev.preventDefault();
    if (!dateEl.value || !timeEl.value) {
      form.reportValidity();
      return;
    }
    const t = `${dateEl.value}T${timeEl.value.slice(0, 5)}`;
    location.href = `plan.html?p=${encodeURIComponent(productId)}&t=${encodeURIComponent(t)}`;
  });
}

function invalidMarkup() {
  return `
    <section class="card">
      <h1>시간표를 만들 수 없습니다</h1>
      <p class="muted">링크가 잘못되었습니다. 제품을 다시 선택해 주세요.</p>
      <a class="btn btn--primary" href="./">제품 선택으로</a>
    </section>`;
}

function renderEvent(e) {
  const end = e.end ? `<small>~${formatTime(e.end)}</small>` : '';
  return `
    <div class="event event--${e.kind}">
      <div class="event__time">${formatTime(e.at)}${end}</div>
      <div>
        <div class="event__title">${esc(e.title)}</div>
        <div class="event__detail">${esc(e.detail)}</div>
      </div>
    </div>`;
}

function renderPlan({ events, warnings, meta }) {
  const { product, examAt } = meta;
  const prevDay = addDays(startOfDay(examAt), -1);
  const group = (day) => events.filter((e) => e.day === day).map(renderEvent).join('');
  const warn = warnings.length
    ? `<div class="banner" role="alert"><ul>${warnings.map((w) => `<li>${esc(w.message)}</li>`).join('')}</ul></div>`
    : '';
  return `
    <section class="hero">
      <h1>${esc(product.name)} 복용 시간표</h1>
      <p class="muted">검사: ${formatDateKo(examAt)} ${formatTime(examAt)} · 2차 복용은 검사 2시간 전에 끝납니다.</p>
      <p class="muted">병원에서 다른 식이·복용 안내를 받았다면 그것을 따르세요.</p>
    </section>
    ${warn}
    <div class="timeline">
      <section class="day">
        <div class="day__title">검사 전날 <span class="muted">${formatDateKo(prevDay)}</span></div>
        ${group('D-1')}
      </section>
      <section class="day">
        <div class="day__title">검사 당일 <span class="muted">${formatDateKo(examAt)}</span></div>
        ${group('D0')}
      </section>
    </div>`;
}

function downloadIcs({ events, meta, tParam }) {
  const ics = buildIcs({ events, uidSeed: `${meta.product.id}-${tParam}` });
  const blob = new Blob([ics], { type: 'text/calendar;charset=utf-8' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = `장비움-${meta.product.short}-${tParam.slice(0, 10)}.ics`;
  document.body.appendChild(a);
  a.click();
  a.remove();
  setTimeout(() => URL.revokeObjectURL(url), 1000);
  toast('캘린더 파일을 내려받았습니다');
}

function initPlanPage() {
  const params = new URLSearchParams(location.search);
  const productId = params.get('p') ?? '';
  const tParam = params.get('t') ?? '';
  const examAt = parseParam(tParam);
  const root = document.getElementById('plan');
  const bar = document.getElementById('bar');

  if (!Object.hasOwn(PRODUCTS, productId) || !examAt) {
    root.innerHTML = invalidMarkup();
    bar.hidden = true;
    return;
  }

  let result;
  try {
    result = computeSchedule({ examAt, productId });
  } catch {
    root.innerHTML = invalidMarkup();
    bar.hidden = true;
    return;
  }
  const { meta } = result;
  document.title = `${meta.product.short} 복용 시간표 · ${formatDateKo(examAt)} ${formatTime(examAt)} | 장비움`;
  root.innerHTML = renderPlan(result);
  const src = document.getElementById('source-line');
  src.innerHTML = `소화기내과 전문의 감수 · 복용법 출처: <a href="${esc(meta.product.source.url)}" rel="noopener" target="_blank">${esc(meta.product.source.label)}</a>`;
  bar.hidden = false;
  document.getElementById('btn-ics').addEventListener('click', () => downloadIcs({ events: result.events, meta, tParam: toParam(examAt) }));
  document.getElementById('btn-share').addEventListener('click', () => share({
    title: `${meta.product.name} 복용 시간표 (${formatDateKo(examAt)} ${formatTime(examAt)} 검사)`,
    url: location.href,
  }));
}
```

- [ ] **Step 3: 기존 테스트와 기존 사이트 동작 확인**

Run: `npm test`
Expected: `pass 37`, `fail 0` (그대로)

브라우저 패널에서 `.claude/launch.json`의 `jangbium` 서버를 띄우고
`http://localhost:8787/coolprep.html`에서 폼을 채워 제출 → `plan.html`에서 결과가 예전과
동일하게 보이는지, "캘린더 저장"과 "링크 공유" 버튼이 여전히 동작하는지 확인한다(콘솔 에러
없어야 함).

- [ ] **Step 4: 커밋**

```bash
git add js/ui-utils.js js/app.js
git commit -m "refactor: esc/toast/share를 js/ui-utils.js로 분리

recall.html·recall-plan.html과 공유하기 위한 사전 정리. 동작 변화 없음.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Task 2: `js/recall.js` — 날짜·표기 도우미

**Files:**
- Create: `js/recall.js`
- Test: `test/recall.test.mjs`

**Interfaces:**
- Consumes: 없음(순수 함수, `js/schedule.js`의 패턴만 참고)
- Produces: `parseDateOnly(s): Date | null`, `formatDateOnly(d): string`,
  `addMonthsClamped(date: Date, months: number): Date`, `formatYearMonthKo(d: Date): string`,
  `formatMonths(n: number): string` — Task 3·4·6·7이 그대로 가져다 쓴다.

- [ ] **Step 1: 실패하는 테스트 작성**

`test/recall.test.mjs` 새로 작성:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import {
  parseDateOnly, formatDateOnly, addMonthsClamped, formatYearMonthKo, formatMonths,
} from '../js/recall.js';

const at = (y, mo, d) => new Date(y, mo - 1, d, 0, 0, 0, 0);

test('parseDateOnly/formatDateOnly', () => {
  const d = parseDateOnly('2026-09-09');
  assert.equal(d.getFullYear(), 2026);
  assert.equal(d.getMonth(), 8);
  assert.equal(d.getDate(), 9);
  assert.equal(formatDateOnly(d), '2026-09-09');
  assert.equal(parseDateOnly(''), null);
  assert.equal(parseDateOnly(null), null);
  assert.equal(parseDateOnly('2026-2-30'), null);
  assert.equal(parseDateOnly('2026-02-30'), null); // 2월 30일은 없음(오버플로 거름)
  assert.equal(parseDateOnly('2026-13-01'), null);
});

test('addMonthsClamped: 월 길이가 다를 때 말일로 클램프', () => {
  assert.equal(formatDateOnly(addMonthsClamped(at(2026, 1, 31), 1)), '2026-02-28'); // 2026은 평년
  assert.equal(formatDateOnly(addMonthsClamped(at(2026, 9, 9), 6)), '2027-03-09');
  assert.equal(formatDateOnly(addMonthsClamped(at(2026, 9, 9), 12)), '2027-09-09');
  assert.equal(formatDateOnly(addMonthsClamped(at(2026, 9, 9), 36)), '2029-09-09');
  assert.equal(formatDateOnly(addMonthsClamped(at(2026, 9, 9), 60)), '2031-09-09');
  assert.equal(formatDateOnly(addMonthsClamped(at(2026, 9, 9), 120)), '2036-09-09');
});

test('formatYearMonthKo', () => {
  assert.equal(formatYearMonthKo(at(2029, 9, 12)), '2029년 9월');
  assert.equal(formatYearMonthKo(at(2031, 1, 1)), '2031년 1월');
});

test('formatMonths: 12로 나뉘면 년, 아니면 개월', () => {
  assert.equal(formatMonths(6), '6개월');
  assert.equal(formatMonths(12), '1년');
  assert.equal(formatMonths(36), '3년');
  assert.equal(formatMonths(60), '5년');
  assert.equal(formatMonths(120), '10년');
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test test/recall.test.mjs`
Expected: FAIL (`js/recall.js` 없음 / export 없음)

- [ ] **Step 3: `js/recall.js` 최소 구현 (1부)**

```js
// 순수 계산 모듈. DOM 없음.
// 근거: 「폴립 절제 후 추적 대장내시경 검사 진료 지침 개정안 2022」
// (대한소화기학회·대한소화기내시경학회·대한장연구학회·대한복부영상의학회,
//  Clinical Endoscopy 2022;55:703-725, doi: 10.5946/ce.2022.136, Table 4)

const pad = (n) => String(n).padStart(2, '0');

export function parseDateOnly(s) {
  const m = /^(\d{4})-(\d{2})-(\d{2})$/.exec(s ?? '');
  if (!m) return null;
  const [, y, mo, d] = m.map(Number);
  if (mo < 1 || mo > 12 || d < 1 || d > 31) return null;
  const date = new Date(y, mo - 1, d, 0, 0, 0, 0);
  if (date.getMonth() !== mo - 1 || date.getDate() !== d) return null;
  return date;
}

export function formatDateOnly(d) {
  return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`;
}

export function addMonthsClamped(date, months) {
  const day = date.getDate();
  const target = new Date(date.getFullYear(), date.getMonth() + months, 1);
  const daysInTarget = new Date(target.getFullYear(), target.getMonth() + 1, 0).getDate();
  target.setDate(Math.min(day, daysInTarget));
  return target;
}

export function formatYearMonthKo(d) {
  return `${d.getFullYear()}년 ${d.getMonth() + 1}월`;
}

export function formatMonths(n) {
  return n % 12 === 0 ? `${n / 12}년` : `${n}개월`;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test test/recall.test.mjs`
Expected: PASS (5개 테스트)

- [ ] **Step 5: 커밋**

```bash
git add js/recall.js test/recall.test.mjs
git commit -m "feat: recall.js 날짜·표기 도우미

addMonthsClamped는 월 길이 차이를 말일로 클램프한다(1/31 +1개월 → 2/28).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Task 3: `js/recall.js` — 추적 간격 계산 엔진

Table 4를 그대로 코드로 옮긴다. 우선순위: 분할절제(6개월) → 선종 10개 초과(1년) → 고위험
소견 하나라도 있음(3년) → 중등도(3~5년) → 저위험(5~10년) → 절제한 것 없음(해당 없음).

**Files:**
- Modify: `js/recall.js`
- Test: `test/recall.test.mjs`

**Interfaces:**
- Consumes: Task 2의 `addMonthsClamped`
- Produces:
  - `FLAG_CHARS: { villous, highGrade, tsa, sslDysplasia, serrated10, piecemeal }` (각 값은
    URL에 쓰는 1글자 코드)
  - `SSL_BANDS: ['0', '1', '3', '5']`
  - `computeRecall({ examDate, adenomaCount, maxSizeMm, flags: Set<string>, sslBand, prepInadequate }): { risk, months: [number,number]|null, dates: Date[], reasons: string[], caveats: string[] }`
  - `buildRecallEvents(result, examDate): Array<{ at, end, calendar: true, title, detail }>`
  - `formatMonthsRange(min: number, max: number): string` — `min === max`면 `formatMonths(min)`
    그대로, 아니면 두 값이 모두 12의 배수일 때만 `"3~5년"`처럼 단위를 한 번만 붙이고, 아니면
    각각 `formatMonths`로 따로 표기해 `~`로 잇는다(예: `"6개월~1년"`). 이 프로젝트가 실제로
    쓰는 범위(36~60, 60~120)는 모두 두 값 다 12의 배수라 항상 "3~5년"/"5~10년" 형태가 된다.
  - `buildReportText(result, examDate): string`
  - Task 4·6·7이 이 여섯 개를 가져다 쓴다.

- [ ] **Step 1: 실패하는 테스트 추가**

`test/recall.test.mjs`에 이어서 추가:

```js
import { FLAG_CHARS, SSL_BANDS, computeRecall, buildRecallEvents, formatMonthsRange, buildReportText } from '../js/recall.js';

const base = { examDate: at(2026, 9, 9), adenomaCount: 0, maxSizeMm: 0, flags: new Set(), sslBand: '0', prepInadequate: false };

test('computeRecall: 절제한 용종 없음', () => {
  const r = computeRecall(base);
  assert.equal(r.risk, 'none');
  assert.equal(r.months, null);
  assert.deepEqual(r.dates, []);
});

test('computeRecall: 저위험 (1~2개, 10mm 미만)', () => {
  const r = computeRecall({ ...base, adenomaCount: 2, maxSizeMm: 8 });
  assert.equal(r.risk, 'low');
  assert.deepEqual(r.months, [60, 120]);
  assert.equal(formatDateOnly(r.dates[0]), '2031-09-09');
  assert.equal(formatDateOnly(r.dates[1]), '2036-09-09');
});

test('computeRecall: 중등도 (선종 3~4개)', () => {
  assert.equal(computeRecall({ ...base, adenomaCount: 3 }).risk, 'moderate');
  assert.deepEqual(computeRecall({ ...base, adenomaCount: 3 }).months, [36, 60]);
  assert.equal(computeRecall({ ...base, adenomaCount: 4 }).risk, 'moderate');
});

test('computeRecall: 고위험 (선종 5~10개)', () => {
  assert.equal(computeRecall({ ...base, adenomaCount: 5 }).risk, 'high');
  assert.deepEqual(computeRecall({ ...base, adenomaCount: 5 }).months, [36, 36]);
  assert.equal(computeRecall({ ...base, adenomaCount: 10 }).risk, 'high');
});

test('computeRecall: 매우 고위험 (선종 10개 초과)', () => {
  const r = computeRecall({ ...base, adenomaCount: 11 });
  assert.equal(r.risk, 'veryhigh');
  assert.deepEqual(r.months, [12, 12]);
  assert.equal(formatDateOnly(r.dates[0]), '2027-09-09');
});

test('computeRecall: 선종 크기 10mm는 개수와 무관하게 고위험 (각주 a)', () => {
  assert.equal(computeRecall({ ...base, adenomaCount: 1, maxSizeMm: 10 }).risk, 'high');
  assert.equal(computeRecall({ ...base, adenomaCount: 1, maxSizeMm: 9 }).risk, 'low');
});

test('computeRecall: 조직 소견 플래그는 개수와 무관하게 고위험', () => {
  for (const key of ['villous', 'highGrade', 'tsa', 'sslDysplasia', 'serrated10']) {
    const r = computeRecall({ ...base, flags: new Set([key]) });
    assert.equal(r.risk, 'high', `${key} 는 고위험이어야 함`);
    assert.ok(r.reasons.length > 0);
  }
});

test('computeRecall: SSL 개수 구간 (각주 b)', () => {
  assert.equal(computeRecall({ ...base, sslBand: '1' }).risk, 'low'); // 1~2개는 위험 소견 아님
  assert.equal(computeRecall({ ...base, sslBand: '3' }).risk, 'moderate');
  assert.deepEqual(computeRecall({ ...base, sslBand: '3' }).months, [36, 60]);
  assert.equal(computeRecall({ ...base, sslBand: '5' }).risk, 'high');
  assert.deepEqual(computeRecall({ ...base, sslBand: '5' }).months, [36, 36]);
});

test('computeRecall: 20mm 이상 분할절제는 다른 소견보다 우선하는 6개월', () => {
  const r = computeRecall({ ...base, adenomaCount: 11, flags: new Set(['piecemeal']) });
  assert.equal(r.risk, 'piecemeal');
  assert.deepEqual(r.months, [6, 6]);
  assert.equal(formatDateOnly(r.dates[0]), '2027-03-09');
  assert.ok(r.caveats.some((c) => c.includes('6개월 이후')));
});

test('computeRecall: 장정결 불량이면 caveat이 붙는다', () => {
  const r = computeRecall({ ...base, adenomaCount: 2, prepInadequate: true });
  assert.ok(r.caveats.some((c) => c.includes('장정결')));
});

test('computeRecall: 잘못된 입력은 던진다', () => {
  assert.throws(() => computeRecall({ ...base, examDate: new Date('invalid') }));
  assert.throws(() => computeRecall({ ...base, adenomaCount: -1 }));
  assert.throws(() => computeRecall({ ...base, sslBand: '2' }));
});

test('buildRecallEvents: 해당 없음이면 이벤트 없음', () => {
  assert.deepEqual(buildRecallEvents(computeRecall(base), base.examDate), []);
});

test('buildRecallEvents: 고위험(3년)은 3개월 전 알림을 만든다', () => {
  const result = computeRecall({ ...base, adenomaCount: 5 });
  const events = buildRecallEvents(result, base.examDate);
  assert.equal(events.length, 2);
  assert.equal(formatDateOnly(events[0].at), '2029-09-09');
  assert.equal(events[0].title, '대장내시경 추적검사 예정');
  assert.equal(formatDateOnly(events[1].at), '2029-06-09');
  assert.equal(events[1].title, '대장내시경 예약할 때');
});

test('buildRecallEvents: 분할절제(6개월)는 1개월 전 알림', () => {
  const result = computeRecall({ ...base, flags: new Set(['piecemeal']) });
  const events = buildRecallEvents(result, base.examDate);
  assert.equal(formatDateOnly(events[0].at), '2027-03-09');
  assert.equal(formatDateOnly(events[1].at), '2027-02-09');
});

test('formatMonthsRange', () => {
  assert.equal(formatMonthsRange(36, 60), '3~5년');
  assert.equal(formatMonthsRange(60, 120), '5~10년');
  assert.equal(formatMonthsRange(6, 6), '6개월');
  assert.equal(formatMonthsRange(12, 12), '1년');
});

test('buildReportText: 결과지 문구', () => {
  const result = computeRecall({ ...base, adenomaCount: 3 });
  const text = buildReportText(result, base.examDate);
  assert.match(text, /3~5년/);
  assert.match(text, /2022 한국/);
  assert.match(text, /2029년 9월/);
});

test('buildReportText: 절제한 용종 없음', () => {
  const text = buildReportText(computeRecall(base), base.examDate);
  assert.match(text, /절제한 용종 없음/);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test test/recall.test.mjs`
Expected: FAIL (`computeRecall`/`buildRecallEvents`/`buildReportText` 없음)

- [ ] **Step 3: `js/recall.js`에 계산 엔진 추가**

Task 2에서 만든 내용 뒤에 이어서 작성:

```js
export const FLAG_CHARS = {
  villous: 'V',
  highGrade: 'H',
  tsa: 'T',
  sslDysplasia: 'D',
  serrated10: 'L',
  piecemeal: 'P',
};

export const SSL_BANDS = ['0', '1', '3', '5']; // 0개 / 1~2개 / 3~4개 / 5개 이상

const FLAG_TEXT = {
  villous: '관융모 또는 융모샘종이 있습니다.',
  highGrade: '고도이형성이 동반되었습니다.',
  tsa: '전통톱니선종(TSA)이 있습니다.',
  sslDysplasia: '이형성이 동반된 톱니병변(SSL)이 있습니다.',
  serrated10: '톱니용종이 10mm 이상입니다.',
};

export function computeRecall({ examDate, adenomaCount = 0, maxSizeMm = 0, flags = new Set(), sslBand = '0', prepInadequate = false }) {
  if (!(examDate instanceof Date) || Number.isNaN(examDate.getTime())) throw new Error('invalid examDate');
  if (!Number.isInteger(adenomaCount) || adenomaCount < 0) throw new Error('invalid adenomaCount');
  if (!Number.isInteger(maxSizeMm) || maxSizeMm < 0) throw new Error('invalid maxSizeMm');
  if (!SSL_BANDS.includes(sslBand)) throw new Error('invalid sslBand');

  const has = (k) => flags.has(k);
  const reasons = [];
  const caveats = [];
  if (prepInadequate) {
    caveats.push('이번 검사의 장정결 상태가 불량했습니다. 추적 간격이 단축될 수 있으니 병원에 확인하세요.');
  }

  let risk, minMonths, maxMonths;

  if (has('piecemeal')) {
    risk = 'piecemeal';
    minMonths = maxMonths = 6;
    reasons.push('20mm 이상의 폴립을 분할 절제했습니다.');
    caveats.push('6개월 이후의 추적 간격은 그때 소견을 반영해 병원에서 다시 정합니다.');
  } else if (adenomaCount > 10) {
    risk = 'veryhigh';
    minMonths = maxMonths = 12;
    reasons.push(`선종이 ${adenomaCount}개로 10개를 초과했습니다.`);
  } else {
    const highReasons = [];
    if (maxSizeMm >= 10) highReasons.push(`선종 최대 크기가 ${maxSizeMm}mm로 10mm 이상입니다.`);
    if (adenomaCount >= 5) highReasons.push(`선종이 ${adenomaCount}개로 5개 이상입니다.`);
    for (const key of ['villous', 'highGrade', 'tsa', 'sslDysplasia', 'serrated10']) {
      if (has(key)) highReasons.push(FLAG_TEXT[key]);
    }
    if (sslBand === '5') highReasons.push('톱니병변(SSL)이 5개 이상입니다.');

    if (highReasons.length > 0) {
      risk = 'high';
      minMonths = maxMonths = 36;
      reasons.push(...highReasons);
    } else {
      const modReasons = [];
      if (adenomaCount >= 3) modReasons.push(`선종이 ${adenomaCount}개입니다.`);
      if (sslBand === '3') modReasons.push('톱니병변(SSL)이 3~4개입니다.');

      if (modReasons.length > 0) {
        risk = 'moderate';
        minMonths = 36;
        maxMonths = 60;
        reasons.push(...modReasons);
      } else if (adenomaCount === 0 && sslBand === '0') {
        risk = 'none';
        minMonths = maxMonths = null;
        reasons.push('이번 검사에서 절제한 용종이 없습니다.');
      } else {
        risk = 'low';
        minMonths = 60;
        maxMonths = 120;
        reasons.push('특별한 위험 소견이 없습니다.');
      }
    }
  }

  const dates =
    risk === 'none' ? []
    : minMonths === maxMonths ? [addMonthsClamped(examDate, minMonths)]
    : [addMonthsClamped(examDate, minMonths), addMonthsClamped(examDate, maxMonths)];

  return { risk, months: risk === 'none' ? null : [minMonths, maxMonths], dates, reasons, caveats };
}

export function buildRecallEvents(result, examDate) {
  if (result.risk === 'none' || result.dates.length === 0) return [];
  const target = result.dates[0];
  const at = new Date(target.getFullYear(), target.getMonth(), target.getDate(), 9, 0, 0, 0);
  const end = new Date(at.getTime() + 30 * 60_000);
  const events = [
    {
      at,
      end,
      calendar: true,
      title: '대장내시경 추적검사 예정',
      detail: `${result.reasons.join(' ')} 병원에서 정해준 날짜가 있으면 그것을 따르세요.`,
    },
  ];
  const reminderMonths = result.months[0] <= 6 ? 1 : 3;
  const remindAt = addMonthsClamped(at, -reminderMonths);
  if (remindAt > examDate) {
    events.push({
      at: remindAt,
      end: new Date(remindAt.getTime() + 30 * 60_000),
      calendar: true,
      title: '대장내시경 예약할 때',
      detail: '다음 대장내시경 추적검사를 예약할 시기입니다.',
    });
  }
  return events;
}

export function formatMonthsRange(min, max) {
  if (min === max) return formatMonths(min);
  if (min % 12 === 0 && max % 12 === 0) return `${min / 12}~${max / 12}년`;
  return `${formatMonths(min)}~${formatMonths(max)}`;
}

export function buildReportText(result, examDate) {
  if (result.risk === 'none') {
    return `${formatDateOnly(examDate)} 검사: 절제한 용종 없음. 일반 검진 주기를 따르세요.`;
  }
  const when = `${formatMonthsRange(result.months[0], result.months[1])} 후`;
  const whenDate =
    result.dates.length > 1 ? `${formatYearMonthKo(result.dates[0])} ~ ${formatYearMonthKo(result.dates[1])}`
    : `${formatYearMonthKo(result.dates[0])}`;
  return `${result.reasons.join(' ')} 2022 한국 폴립절제 후 추적 대장내시경 검사 지침에 따라 ${when} 추적 대장내시경을 권고합니다 (${whenDate}).`;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test test/recall.test.mjs`
Expected: PASS (전체)

- [ ] **Step 5: 커밋**

```bash
git add js/recall.js test/recall.test.mjs
git commit -m "feat: recall.js 추적 간격 계산 엔진 (2022 한국 지침 Table 4)

분할절제(6개월) > 선종 10개 초과(1년) > 고위험 소견(3년) > 중등도(3~5년) >
저위험(5~10년) > 절제한 것 없음 순으로 판정. 결과지 문구와 캘린더 이벤트도
같은 모듈에서 만든다.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Task 4: `js/recall.js` — URL 파라미터 파싱·생성

**Files:**
- Modify: `js/recall.js`
- Test: `test/recall.test.mjs`

**Interfaces:**
- Consumes: Task 2·3의 `parseDateOnly`, `formatDateOnly`, `FLAG_CHARS`, `SSL_BANDS`
- Produces: `parseRecallParams(params: URLSearchParams): RecallInput | null`,
  `buildRecallQuery(input: RecallInput): string` (쿼리 문자열, 맨 앞 `?` 없음) — Task 6·7이 쓴다.
  `RecallInput`은 `computeRecall`의 인자와 같은 모양: `{ examDate, adenomaCount, maxSizeMm, flags, sslBand, prepInadequate }`.

- [ ] **Step 1: 실패하는 테스트 추가**

```js
import { parseRecallParams, buildRecallQuery } from '../js/recall.js';

test('buildRecallQuery ↔ parseRecallParams 왕복', () => {
  const input = {
    examDate: at(2026, 9, 9),
    adenomaCount: 4,
    maxSizeMm: 8,
    flags: new Set(['villous', 'piecemeal']),
    sslBand: '3',
    prepInadequate: true,
  };
  const query = buildRecallQuery(input);
  const parsed = parseRecallParams(new URLSearchParams(query));
  assert.equal(formatDateOnly(parsed.examDate), '2026-09-09');
  assert.equal(parsed.adenomaCount, 4);
  assert.equal(parsed.maxSizeMm, 8);
  assert.deepEqual([...parsed.flags].sort(), ['piecemeal', 'villous']);
  assert.equal(parsed.sslBand, '3');
  assert.equal(parsed.prepInadequate, true);
});

test('buildRecallQuery: 기본값(플래그 없음, ssl 0, 불량 아님)은 짧게', () => {
  const query = buildRecallQuery({ examDate: at(2026, 9, 9), adenomaCount: 0, maxSizeMm: 0, flags: new Set(), sslBand: '0', prepInadequate: false });
  assert.equal(query, 'd=2026-09-09&n=0&s=0');
});

test('parseRecallParams: 잘못된 값은 null', () => {
  const bad = (s) => parseRecallParams(new URLSearchParams(s));
  assert.equal(bad(''), null); // 날짜 없음
  assert.equal(bad('d=2026-13-01&n=0&s=0'), null); // 잘못된 날짜
  assert.equal(bad('d=2026-09-09&n=abc&s=0'), null); // 개수 아님
  assert.equal(bad('d=2026-09-09&n=0&s=0&f=Z'), null); // 알 수 없는 플래그
  assert.equal(bad('d=2026-09-09&n=0&s=0&ssl=2'), null); // 잘못된 ssl 구간
  assert.equal(bad('d=2026-09-09&n=0&s=0&b=9'), null); // 잘못된 장정결 값
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test test/recall.test.mjs`
Expected: FAIL

- [ ] **Step 3: `js/recall.js`에 파싱·생성 함수 추가**

```js
export function parseRecallParams(params) {
  const examDate = parseDateOnly(params.get('d'));
  if (!examDate) return null;

  const nRaw = params.get('n');
  if (!/^\d{1,3}$/.test(nRaw ?? '')) return null;
  const adenomaCount = Number(nRaw);

  const sRaw = params.get('s');
  if (!/^\d{1,3}$/.test(sRaw ?? '')) return null;
  const maxSizeMm = Number(sRaw);

  const fRaw = params.get('f') ?? '';
  const validChars = new Set(Object.values(FLAG_CHARS));
  if ([...fRaw].some((c) => !validChars.has(c))) return null;
  const flags = new Set();
  for (const [key, ch] of Object.entries(FLAG_CHARS)) {
    if (fRaw.includes(ch)) flags.add(key);
  }

  const sslBand = params.get('ssl') ?? '0';
  if (!SSL_BANDS.includes(sslBand)) return null;

  const bRaw = params.get('b') ?? '0';
  if (bRaw !== '0' && bRaw !== '1') return null;

  return { examDate, adenomaCount, maxSizeMm, flags, sslBand, prepInadequate: bRaw === '1' };
}

export function buildRecallQuery(input) {
  const p = new URLSearchParams();
  p.set('d', formatDateOnly(input.examDate));
  p.set('n', String(input.adenomaCount));
  p.set('s', String(input.maxSizeMm));
  const f = Object.entries(FLAG_CHARS).filter(([key]) => input.flags.has(key)).map(([, ch]) => ch).join('');
  if (f) p.set('f', f);
  if (input.sslBand && input.sslBand !== '0') p.set('ssl', input.sslBand);
  if (input.prepInadequate) p.set('b', '1');
  return p.toString();
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test test/recall.test.mjs`
Expected: PASS (전체 — 이 시점까지 recall.js 관련 테스트 총 24개 안팎)

Run: `npm test`
Expected: `pass` 합계가 기존 37 + 이번에 추가한 recall.js 테스트 수만큼 늘어나고 `fail 0`

- [ ] **Step 5: 커밋**

```bash
git add js/recall.js test/recall.test.mjs
git commit -m "feat: recall.js URL 파라미터 파싱·생성

recall-plan.html?d=&n=&s=&f=&ssl=&b= 스킴. plan.html의 Object.hasOwn 검증
패턴과 동일하게, 잘못된 값은 예외 대신 null을 돌려준다.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Task 5: QR 라이브러리 벤더링 (`js/vendor/qrcode.js`)

환자 화면(Task 7)에서 결과 URL을 QR로 보여주기 위해 MIT 라이선스 QR 인코더를 그대로
가져온다. 직접 작성하지 않고 커밋 SHA와 체크섬을 고정해 내려받는다 — QR 인코딩(리드-솔로몬
오류정정 포함)은 새로 짜기엔 위험이 크고, 이 라이브러리는 UMD라 `<script>` 전역으로 그대로
쓸 수 있다.

**Files:**
- Create: `js/vendor/qrcode.js`
- Create: `js/vendor/LICENSE-qrcode.txt`

**Interfaces:**
- Produces: 전역 `window.qrcode(typeNumber, errorCorrectionLevel)` 팩토리(고전 `<script>`로
  로드했을 때). `qrcode(0, 'M').addData(url); .make(); .createSvgTag(cellSize, margin)`가
  완결된 `<svg>...</svg>` 문자열을 돌려준다. Task 7의 `recall-app.js`가 `window.qrcode`로
  사용한다.

- [ ] **Step 1: 디렉터리 만들고 고정 커밋에서 내려받기**

출처: https://github.com/kazuhikoarase/qrcode-generator (MIT, Copyright (c) 2009 Kazuhiko Arase)
고정 커밋: `64f5976e5f9256348d0f5417ceff934bb43cf279` (2025-08-07)
파일: `js/dist/qrcode.js` (56,694 bytes)

```bash
mkdir -p js/vendor
curl -sL "https://raw.githubusercontent.com/kazuhikoarase/qrcode-generator/64f5976e5f9256348d0f5417ceff934bb43cf279/js/dist/qrcode.js" -o js/vendor/qrcode.js
curl -sL "https://raw.githubusercontent.com/kazuhikoarase/qrcode-generator/64f5976e5f9256348d0f5417ceff934bb43cf279/LICENSE" -o js/vendor/LICENSE-qrcode.txt
```

- [ ] **Step 2: 체크섬으로 내려받은 내용이 맞는지 확인**

Run:
```bash
node -e "
const fs = require('fs'), crypto = require('crypto');
const buf = fs.readFileSync('js/vendor/qrcode.js');
console.log('size', buf.length);
console.log('sha256', crypto.createHash('sha256').update(buf).digest('hex'));
"
```
Expected: `size 56694`, `sha256 79ec86f82856005b1c887905cfccfcfbec3821ca61c7fd5a952faa5f778f791c`

다르면 내려받기가 손상됐거나 URL이 바뀐 것이니 다시 받는다. 파일을 손으로 고치지 않는다.

- [ ] **Step 3: 파일 맨 위에 출처 주석 한 줄 추가**

`js/vendor/qrcode.js` 맨 위(기존 첫 줄 앞)에 아래 한 줄을 추가한다(라이브러리 원문은
그대로 두고 출처만 덧붙인다):

```js
// 벤더 카피: https://github.com/kazuhikoarase/qrcode-generator (MIT) @ 64f5976e5f9256348d0f5417ceff934bb43cf279/js/dist/qrcode.js — 라이선스는 LICENSE-qrcode.txt 참고. 손으로 고치지 않는다.
```

- [ ] **Step 4: 동작 검증 (Node 샌드박스 스모크 테스트)**

이 파일은 UMD 스크립트라 `"type": "module"`인 이 프로젝트에서 `import`/`require`로 바로
못 부른다. 실제 사용처(고전 `<script>` 태그)와 같은 방식으로 동작하는지 아래로 한 번
확인한다(자동화된 `node --test` 스위트에는 넣지 않는다 — 벤더 코드는 우리가 짠 로직이
아니므로 `js/app.js`와 같은 방식으로 브라우저 검증 대상이다):

```bash
node -e "
const fs = require('fs');
const src = fs.readFileSync('js/vendor/qrcode.js', 'utf8');
const sandbox = { module: { exports: {} }, exports: {} };
new Function('module', 'exports', src)(sandbox.module, sandbox.exports);
const qrcode = sandbox.module.exports;
const qr = qrcode(0, 'M');
qr.addData('https://jangbium.github.io/recall-plan.html?d=2026-09-09&n=3&s=8');
qr.make();
const svg = qr.createSvgTag(4, 8);
if (!svg.startsWith('<svg') || qr.getModuleCount() < 20) throw new Error('QR 검증 실패');
console.log('QR 라이브러리 검증 통과, moduleCount=' + qr.getModuleCount());
"
```
Expected: `QR 라이브러리 검증 통과, moduleCount=37` (URL 길이에 따라 34~41 사이일 수 있음 — 20 이상이면 정상)

- [ ] **Step 5: 커밋**

```bash
git add js/vendor/qrcode.js js/vendor/LICENSE-qrcode.txt
git commit -m "chore: QR 인코더 벤더링 (kazuhikoarase/qrcode-generator, MIT)

환자용 추적검사 링크를 QR로 보여주기 위해. 커밋 64f5976 고정, 체크섬 확인.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Task 6: `recall.html` — 의사 입력 화면

소견을 체크하면 즉시 결과가 갱신되는 화면. 페이지 이동 없음.

**Files:**
- Create: `recall.html`
- Create: `js/recall-app.js`
- Modify: `css/style.css` (칩·결과 카드 스타일 추가)

**Interfaces:**
- Consumes: Task 1의 `esc, toast, copyText`(`ui-utils.js`), Task 2-4의
  `computeRecall, buildReportText, formatYearMonthKo, formatMonthsRange, parseDateOnly,
  formatDateOnly, buildRecallQuery`(`recall.js`)
- Produces: `js/recall-app.js`는 `document.body.dataset.page === 'recall'`일 때
  `initRecallForm()`을 실행한다. Task 7이 같은 파일에 `initRecallPlanPage()`를 추가한다.

- [ ] **Step 1: `css/style.css` 끝에 칩·결과 카드 스타일 추가**

`css/style.css` 맨 끝(`@media (prefers-reduced-motion: reduce) { ... }` 블록 뒤)에 추가:

```css

.chip {
  display: inline-flex; align-items: center; border: 1px solid var(--line); background: var(--surface-solid);
  color: var(--ink); border-radius: 999px; padding: 8px 14px; margin: 0 8px 8px 0; font: inherit; font-weight: 600;
  font-size: 0.95rem; cursor: pointer;
}
.chip.on { background: var(--accent); color: var(--accent-ink); border-color: var(--accent); }
.chip:focus-visible { outline: 3px solid var(--accent); outline-offset: 2px; }

.tag { display: inline-block; font-size: 0.75rem; font-weight: 700; color: var(--accent-ink); background: var(--accent); padding: 2px 10px; border-radius: 6px; margin-bottom: 8px; }
.result .big { font-size: 2rem; font-weight: 800; line-height: 1.25; margin: 6px 0; }
.result .big small { display: block; font-size: 1rem; font-weight: 600; color: var(--ink-2); margin-top: 2px; }
.why { margin: 10px 0 0; padding-left: 18px; color: var(--ink-2); font-size: 0.95rem; }
.why li { margin: 2px 0; }
.copybox { background: var(--surface-solid); border: 1px dashed var(--line); border-radius: 10px; padding: 10px 12px; font-size: 0.95rem; margin-top: 12px; }
#qr-holder svg { display: block; margin: 0 auto; max-width: 220px; height: auto; }
```

- [ ] **Step 2: `recall.html` 작성**

```html
<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>대장내시경 추적검사 계산기 | 장비움</title>
<meta name="description" content="용종 절제 소견을 입력하면 2022년 한국 지침 기준 다음 대장내시경 추적검사 시기를 계산합니다. 결과지 문구와 환자용 QR 링크를 만듭니다.">
<link rel="canonical" href="https://jangbium.github.io/recall.html">
<meta property="og:type" content="website">
<meta property="og:title" content="대장내시경 추적검사 계산기 | 장비움">
<meta property="og:description" content="용종 절제 소견으로 다음 검사 시기를 계산합니다.">
<meta property="og:image" content="https://jangbium.github.io/og.png">
<meta property="og:url" content="https://jangbium.github.io/recall.html">
<link rel="stylesheet" href="css/style.css">
<script type="module" src="js/recall-app.js"></script>
</head>
<body data-page="recall">
<header class="site-header">
  <div class="wrap">
    <a class="brand" href="./">장비움<small>추적검사 계산기</small></a>
  </div>
</header>

<main class="wrap">
  <section class="hero">
    <h1>대장내시경 다음 검사 시기</h1>
    <p class="muted">이번 검사 소견을 입력하면 2022년 한국 폴립절제 후 추적 대장내시경 검사 지침 기준으로 다음 검사 시기를 계산합니다.</p>
  </section>

  <form id="recall-form" class="card" novalidate>
    <h2>이번 검사</h2>
    <div class="row">
      <div class="field">
        <label for="r-date">검사일</label>
        <input type="date" id="r-date" name="r-date">
      </div>
      <div class="field">
        <label for="r-prep">장정결</label>
        <select id="r-prep" name="r-prep">
          <option value="0" selected>적절</option>
          <option value="1">불량</option>
        </select>
      </div>
    </div>

    <h2>선종(adenoma)</h2>
    <div class="row">
      <div class="field">
        <label for="r-count">개수</label>
        <input type="number" id="r-count" name="r-count" min="0" max="99" value="0" inputmode="numeric">
      </div>
      <div class="field">
        <label for="r-size">가장 큰 크기(mm)</label>
        <input type="number" id="r-size" name="r-size" min="0" max="99" value="0" inputmode="numeric">
      </div>
    </div>
    <div class="chips" role="group" aria-label="선종 조직 소견">
      <button type="button" class="chip" data-flag="villous">관융모 또는 융모</button>
      <button type="button" class="chip" data-flag="highGrade">고도이형성</button>
    </div>

    <h2>톱니용종(serrated polyp)</h2>
    <div class="chips" role="group" aria-label="톱니용종 소견">
      <button type="button" class="chip" data-flag="tsa">전통톱니선종(TSA)</button>
      <button type="button" class="chip" data-flag="sslDysplasia">이형성 동반 SSL</button>
      <button type="button" class="chip" data-flag="serrated10">톱니용종 10mm 이상</button>
    </div>
    <div class="field">
      <label for="r-ssl">SSL(무경성 톱니병변) 개수</label>
      <select id="r-ssl" name="r-ssl">
        <option value="0" selected>없음</option>
        <option value="1">1~2개</option>
        <option value="3">3~4개</option>
        <option value="5">5개 이상</option>
      </select>
    </div>

    <h2>절제</h2>
    <div class="chips" role="group" aria-label="절제 소견">
      <button type="button" class="chip" data-flag="piecemeal">20mm 이상 분할절제</button>
    </div>
  </form>

  <section class="card result" id="recall-result" aria-live="polite"></section>

  <section class="card">
    <h2>이 계산기가 하는 일</h2>
    <p>2022년 한국 폴립절제 후 추적 대장내시경 검사 지침(Table 4)의 추적 간격을 그대로 계산합니다. 근거수준·권고등급이 서로 다른 여러 소견 중 가장 짧은 간격을 우선합니다.</p>
    <p class="muted">병원에서 개별적으로 정한 추적 시기가 있으면 그것이 우선입니다. 이 도구는 진단·처방이 아닙니다.</p>
  </section>
</main>

<div class="bar" id="bar" hidden>
  <div class="wrap">
    <button type="button" class="btn" id="btn-copy">문구 복사</button>
    <button type="button" class="btn btn--primary" id="btn-patient-link">환자용 링크</button>
  </div>
</div>
<div class="toast" id="toast" role="status"></div>

<footer class="site-footer">
  <div class="wrap">
    <p>이 도구는 정보 제공 목적이며 진단·처방이 아닙니다. 병원에서 받은 안내가 있으면 그것을 따르세요.</p>
    <p>소화기내과 전문의 감수 · 근거: <a href="https://www.ekjm.org/journal/view.php?number=25839" rel="noopener" target="_blank">폴립 절제 후 추적 대장내시경 검사 진료 지침 개정안 2022</a></p>
  </div>
</footer>
</body>
</html>
```

- [ ] **Step 3: `js/recall-app.js` 작성 (의사 화면 부분)**

```js
import {
  computeRecall, buildReportText, formatYearMonthKo, formatMonthsRange,
  parseDateOnly, formatDateOnly, buildRecallQuery,
} from './recall.js';
import { esc, toast, copyText } from './ui-utils.js';

document.addEventListener('DOMContentLoaded', () => {
  const page = document.body.dataset.page;
  if (page === 'recall') initRecallForm();
});

const RISK_LABEL = {
  none: '해당 없음',
  low: '저위험',
  moderate: '중등도 위험',
  high: '고위험',
  veryhigh: '매우 고위험',
  piecemeal: '분할절제 후',
};

function formatRange(result) {
  const big = `${formatMonthsRange(result.months[0], result.months[1])} 뒤`;
  const dateLine =
    result.dates.length > 1 ? `${formatYearMonthKo(result.dates[0])} ~ ${formatYearMonthKo(result.dates[1])}`
    : `${formatYearMonthKo(result.dates[0])} 무렵`;
  return { big, dateLine };
}

function renderResultCard(el, input, result) {
  if (result.risk === 'none') {
    el.innerHTML = `
      <span class="tag">${esc(RISK_LABEL.none)}</span>
      <p class="muted">${esc(result.reasons[0])} 일반 검진 주기를 따르세요.</p>`;
    return;
  }
  const { big, dateLine } = formatRange(result);
  const reasons = result.reasons.map((r) => `<li>${esc(r)}</li>`).join('');
  const caveats = result.caveats.length
    ? `<div class="banner"><ul>${result.caveats.map((c) => `<li>${esc(c)}</li>`).join('')}</ul></div>`
    : '';
  el.innerHTML = `
    <span class="tag">${esc(RISK_LABEL[result.risk])}</span>
    <div class="big">${esc(big)}<small>${esc(dateLine)}</small></div>
    <ul class="why">${reasons}</ul>
    ${caveats}
    <div class="copybox">${esc(buildReportText(result, input.examDate))}</div>`;
}

function readInput(form) {
  const flags = new Set();
  form.querySelectorAll('.chip.on').forEach((btn) => flags.add(btn.dataset.flag));
  return {
    examDate: parseDateOnly(form.elements['r-date'].value) ?? new Date(),
    adenomaCount: Math.max(0, Math.trunc(Number(form.elements['r-count'].value)) || 0),
    maxSizeMm: Math.max(0, Math.trunc(Number(form.elements['r-size'].value)) || 0),
    flags,
    sslBand: form.elements['r-ssl'].value,
    prepInadequate: form.elements['r-prep'].value === '1',
  };
}

function initRecallForm() {
  const form = document.getElementById('recall-form');
  const resultEl = document.getElementById('recall-result');
  const bar = document.getElementById('bar');
  const dateEl = form.elements['r-date'];
  dateEl.max = formatDateOnly(new Date());
  if (!dateEl.value) dateEl.value = formatDateOnly(new Date());

  let lastInput = null;
  let lastResult = null;

  function update() {
    lastInput = readInput(form);
    lastResult = computeRecall(lastInput);
    renderResultCard(resultEl, lastInput, lastResult);
    bar.hidden = false;
  }

  form.addEventListener('input', update);
  form.addEventListener('change', update);
  form.querySelectorAll('.chip').forEach((btn) => {
    btn.addEventListener('click', () => {
      btn.classList.toggle('on');
      update();
    });
  });
  update();

  document.getElementById('btn-copy').addEventListener('click', () => {
    if (!lastResult) return;
    copyText(buildReportText(lastResult, lastInput.examDate), '결과지 문구를 복사했습니다');
  });
  document.getElementById('btn-patient-link').addEventListener('click', () => {
    if (!lastInput) return;
    const base = location.href.replace(/recall\.html.*$/, '');
    const url = `${base}recall-plan.html?${buildRecallQuery(lastInput)}`;
    window.open(url, '_blank', 'noopener');
    copyText(url, '환자용 링크를 복사했습니다');
  });
}
```

- [ ] **Step 4: 브라우저로 검증**

`.claude/launch.json`의 `jangbium` 서버로 `http://localhost:8787/recall.html`을 연다.

1. 선종 개수를 0→2→3→5→11로 바꾸며 결과 카드의 위험군 태그·기간·날짜가 저위험→중등도→
   고위험→매우고위험 순으로 바뀌는지 확인한다.
2. "20mm 이상 분할절제" 칩을 누르면 다른 값과 무관하게 "6개월 뒤"로 바뀌는지 확인한다.
3. "문구 복사"를 누르고 토스트가 뜨는지 확인한다.
4. "환자용 링크"를 누르면 새 탭이 `recall-plan.html?...`로 열리는지 확인한다(Task 7 전이라
   "링크가 잘못되었습니다" 화면이 뜨는 것은 정상 — 다음 태스크에서 만든다).
5. `read_console_messages`로 에러 없는지 확인한다.

- [ ] **Step 5: 커밋**

```bash
git add recall.html js/recall-app.js css/style.css
git commit -m "feat: recall.html 의사 입력 화면

소견 체크마다 즉시 결과가 갱신된다. 결과지 문구 복사와 환자용 링크 생성을
제공한다.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Task 7: `recall-plan.html` — 환자 화면 (QR·캘린더·공유)

**Files:**
- Create: `recall-plan.html`
- Modify: `js/recall-app.js` (환자 화면 부분 추가)

**Interfaces:**
- Consumes: Task 1의 `esc, toast, share`(`ui-utils.js`), Task 3-4의 `computeRecall,
  buildRecallEvents, parseRecallParams, formatDateOnly`(`recall.js`), 기존 `buildIcs`(`js/ics.js`),
  Task 5의 전역 `window.qrcode`
- Produces: `initRecallPlanPage()` — `document.body.dataset.page === 'recall-plan'`일 때 실행

- [ ] **Step 1: `recall-plan.html` 작성**

`plan.html`과 동일하게 URL 파라미터만으로 렌더하고 검색엔진에는 잡히지 않게 한다.

```html
<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>대장내시경 다음 검사 시기 | 장비움</title>
<meta name="robots" content="noindex">
<meta name="description" content="대장내시경 다음 검사 시기">
<meta property="og:type" content="website">
<meta property="og:title" content="대장내시경 다음 검사 시기 | 장비움">
<meta property="og:description" content="용종 절제 소견에 따른 다음 검사 시기입니다.">
<meta property="og:image" content="https://jangbium.github.io/og.png">
<link rel="stylesheet" href="css/style.css">
<script src="js/vendor/qrcode.js"></script>
<script type="module" src="js/recall-app.js"></script>
</head>
<body data-page="recall-plan">
<header class="site-header">
  <div class="wrap">
    <a class="brand" href="./">장비움<small>추적검사 계산기</small></a>
  </div>
</header>

<main class="wrap">
  <div id="plan" aria-live="polite">
    <p class="muted">계산하는 중…</p>
  </div>
</main>

<div class="bar" id="bar" hidden>
  <div class="wrap">
    <button type="button" class="btn" id="btn-ics">캘린더 저장</button>
    <button type="button" class="btn btn--primary" id="btn-share">링크 공유</button>
  </div>
</div>
<div class="toast" id="toast" role="status"></div>

<footer class="site-footer">
  <div class="wrap">
    <p>이 도구는 정보 제공 목적이며 진단·처방이 아닙니다. 병원에서 받은 안내가 있으면 그것을 따르세요.</p>
    <p>소화기내과 전문의 감수 · 근거: <a href="https://www.ekjm.org/journal/view.php?number=25839" rel="noopener" target="_blank">폴립 절제 후 추적 대장내시경 검사 진료 지침 개정안 2022</a></p>
  </div>
</footer>
</body>
</html>
```

- [ ] **Step 2: `js/recall-app.js`에 환자 화면 로직 추가**

파일 맨 위, Task 6이 써 둔 두 개의 `import` 문을 아래로 교체한다(새로 추가되는 이름은
`buildRecallEvents`, `parseRecallParams`, `share`뿐이고 나머지는 Task 6 것을 그대로
유지한다 — import 문을 두 번 따로 쓰지 않는다):

```js
import {
  computeRecall, buildReportText, buildRecallEvents, formatYearMonthKo, formatMonthsRange,
  parseDateOnly, formatDateOnly, buildRecallQuery, parseRecallParams,
} from './recall.js';
import { buildIcs } from './ics.js';
import { esc, toast, share, copyText } from './ui-utils.js';
```

`DOMContentLoaded` 리스너를 아래로 교체:

```js
document.addEventListener('DOMContentLoaded', () => {
  const page = document.body.dataset.page;
  if (page === 'recall') initRecallForm();
  if (page === 'recall-plan') initRecallPlanPage();
});
```

파일 맨 끝에 추가:

```js
function invalidRecallMarkup() {
  return `
    <section class="card">
      <h1>다음 검사 시기를 계산할 수 없습니다</h1>
      <p class="muted">링크가 잘못되었습니다. 병원에서 다시 링크를 받아 주세요.</p>
      <a class="btn btn--primary" href="recall.html">계산기로</a>
    </section>`;
}

function renderQr(url) {
  const holder = document.getElementById('qr-holder');
  if (!holder) return;
  if (typeof window.qrcode !== 'function') {
    holder.innerHTML = `<p class="muted">QR을 표시할 수 없습니다. 아래 링크를 사용하세요.</p><p class="muted">${esc(url)}</p>`;
    return;
  }
  const qr = window.qrcode(0, 'M');
  qr.addData(url);
  qr.make();
  holder.innerHTML = qr.createSvgTag(4, 8);
}

function renderPatientPlan(result) {
  const root = document.getElementById('plan');
  if (result.risk === 'none') {
    root.innerHTML = `
      <section class="hero">
        <h1>이번 계산기의 대상이 아닙니다</h1>
        <p class="muted">${esc(result.reasons[0])} 일반 검진 주기를 따르세요.</p>
        <p class="muted">병원에서 따로 정해준 날짜가 있으면 그것을 따르세요.</p>
      </section>`;
    return;
  }
  const { big, dateLine } = formatRange(result);
  const caveats = result.caveats.length
    ? `<div class="banner"><ul>${result.caveats.map((c) => `<li>${esc(c)}</li>`).join('')}</ul></div>`
    : '';
  root.innerHTML = `
    <section class="hero">
      <h1>다음 대장내시경은 ${esc(big)}</h1>
      <p class="muted">${esc(dateLine)}</p>
      <p>${esc(result.reasons.join(' '))} 2022년 한국 폴립절제 후 추적 대장내시경 검사 지침에 따른 계산입니다.</p>
      <p class="muted">병원에서 따로 정해준 날짜가 있으면 그것을 따르세요.</p>
    </section>
    ${caveats}
    <section class="card">
      <p class="muted">이 화면을 QR로 저장하거나 인쇄할 수 있습니다.</p>
      <div id="qr-holder"></div>
    </section>`;
  renderQr(location.href);
}

function downloadRecallIcs({ result, input }) {
  const events = buildRecallEvents(result, input.examDate);
  const ics = buildIcs({ events, uidSeed: `recall-${formatDateOnly(input.examDate)}-${result.risk}` });
  const blob = new Blob([ics], { type: 'text/calendar;charset=utf-8' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = `장비움-추적검사-${formatDateOnly(input.examDate)}.ics`;
  document.body.appendChild(a);
  a.click();
  a.remove();
  setTimeout(() => URL.revokeObjectURL(url), 1000);
  toast('캘린더 파일을 내려받았습니다');
}

function initRecallPlanPage() {
  const params = new URLSearchParams(location.search);
  const input = parseRecallParams(params);
  const root = document.getElementById('plan');
  const bar = document.getElementById('bar');

  if (!input) {
    root.innerHTML = invalidRecallMarkup();
    bar.hidden = true;
    return;
  }

  let result;
  try {
    result = computeRecall(input);
  } catch {
    root.innerHTML = invalidRecallMarkup();
    bar.hidden = true;
    return;
  }

  renderPatientPlan(result);

  if (result.risk === 'none') {
    bar.hidden = true;
    return;
  }
  bar.hidden = false;
  document.getElementById('btn-ics').addEventListener('click', () => downloadRecallIcs({ result, input }));
  document.getElementById('btn-share').addEventListener('click', () => share({
    title: '대장내시경 다음 검사 시기 | 장비움',
    url: location.href,
  }));
}
```

- [ ] **Step 3: 브라우저로 전체 흐름 검증**

1. `http://localhost:8787/recall.html`에서 선종 3개로 입력하고 "환자용 링크"를 눌러 새로
   열린 `recall-plan.html?...` 탭으로 이동한다.
2. 환자 화면에 "다음 대장내시경은 3~5년 뒤"와 QR SVG가 보이는지 확인한다(`read_page`로
   `<svg` 존재 확인, `zoom`으로 QR이 격자 무늬로 그려졌는지 육안 확인).
3. "캘린더 저장"을 눌러 `.ics` 파일이 받아지는지 확인하고, 내용을 열어
   `SUMMARY:[장비움] 대장내시경 추적검사 예정`과 `DTSTART;TZID=Asia/Seoul:20290909T090000`
   (선종 3개 예시라면 2029-09-09)이 들어있는지 확인한다.
4. `recall-plan.html?d=2026-09-09&n=0&s=0` (용종 없음)로 직접 접속해 "이 계산기의 대상이
   아닙니다" 화면과 하단 바 숨김을 확인한다.
5. `recall-plan.html?d=bad`처럼 깨진 링크로 접속해 "링크가 잘못되었습니다" 화면을 확인한다.
6. `resize_window`로 다크 모드(`colorScheme: 'dark'`)에서도 QR 카드와 칩 대비가 괜찮은지
   확인한다.
7. `read_console_messages`로 에러 없는지 최종 확인한다.

- [ ] **Step 4: 커밋**

```bash
git add recall-plan.html js/recall-app.js
git commit -m "feat: recall-plan.html 환자 화면 (QR·캘린더 저장·공유)

의사 화면에서 만든 링크를 열면 다음 검사 시기와 QR, 캘린더 저장·공유 버튼을
보여준다. 용종이 없거나 링크가 잘못되면 각각 안내 화면으로 처리한다.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Task 8: 사이트 연결·배포 설정·최종 검증

**Files:**
- Modify: `index.html`
- Modify: `sitemap.xml`
- Modify: `README.md`
- Modify: `tools/deploy-pages.sh`
- Modify: `.github/workflows/deploy.yml`

**Interfaces:**
- Consumes: 없음 (배선 작업)
- Produces: 없음 (마지막 태스크)

- [ ] **Step 1: `index.html`에 카드 추가**

`index.html`의 기존 `grid grid--3` 섹션(`</section>`로 끝나는 부분) 바로 뒤, "이 도구가
하는 일" 카드 앞에 추가:

```html
  <a class="card card--link" href="recall.html">
    <h2>추적검사 계산기</h2>
    <p class="muted">용종을 절제했다면, 다음 대장내시경은 언제 받아야 하는지 계산합니다.</p>
  </a>
```

- [ ] **Step 2: `sitemap.xml`에 한 줄 추가**

`sitemap.xml`의 `plenvu.html` 항목 뒤, `</urlset>` 앞에 추가(환자용 `recall-plan.html`은
`noindex`라 sitemap에 넣지 않는다 — `plan.html`도 동일한 이유로 빠져 있다):

```xml
  <url><loc>https://jangbium.github.io/recall.html</loc><changefreq>monthly</changefreq><priority>0.9</priority></url>
```

- [ ] **Step 3: `README.md` 구조 목록에 추가**

`README.md`의 `## 구조` 목록은 현재 이렇게 끝난다:

```markdown
- `js/app.js` — 폼과 결과 렌더, 공유·저장 버튼.
- `index.html`, `coolprep.html`, `orapang.html`, `plenvu.html`, `plan.html`
```

이 두 줄을 아래로 교체한다(`js/app.js` 줄 다음에 새 모듈 3개를 끼워 넣고, 마지막 줄의
페이지 목록에 `recall.html`, `recall-plan.html`을 더한다):

```markdown
- `js/app.js` — 폼과 결과 렌더, 공유·저장 버튼.
- `js/ui-utils.js` — esc·toast·share 등 공용 DOM 도우미(app.js·recall-app.js 공유).
- `js/recall.js` — 순수 계산: 2022 한국 지침 Table 4 기준 추적 간격.
- `js/recall-app.js` — 추적검사 계산기 폼과 결과 렌더.
- `js/vendor/qrcode.js` — 벤더링한 QR 인코더(MIT, 손으로 고치지 않음).
- `index.html`, `coolprep.html`, `orapang.html`, `plenvu.html`, `plan.html`, `recall.html`, `recall-plan.html`
```

- [ ] **Step 4: `tools/deploy-pages.sh`의 FILES 배열에 추가**

`tools/deploy-pages.sh`에서:

```bash
FILES=(index.html coolprep.html orapang.html plenvu.html plan.html og.png robots.txt sitemap.xml README.md)
```

를 아래로 교체:

```bash
FILES=(index.html coolprep.html orapang.html plenvu.html plan.html recall.html recall-plan.html og.png robots.txt sitemap.xml README.md)
```

(`css`, `js` 디렉터리는 이미 통째로 복사되므로 `js/vendor/qrcode.js`는 자동으로 따라간다.)

- [ ] **Step 5: `.github/workflows/deploy.yml`의 복사 목록에 추가**

`.github/workflows/deploy.yml`에서:

```yaml
          cp index.html coolprep.html orapang.html plenvu.html plan.html og.png robots.txt sitemap.xml README.md /tmp/deploy/
```

를 아래로 교체:

```yaml
          cp index.html coolprep.html orapang.html plenvu.html plan.html recall.html recall-plan.html og.png robots.txt sitemap.xml README.md /tmp/deploy/
```

- [ ] **Step 6: 전체 테스트·로컬 확인**

Run: `npm test`
Expected: `fail 0`, `pass`는 Task 2-4에서 추가한 recall.js 테스트를 포함해 기존 37개보다
많다.

브라우저 패널에서 `http://localhost:8787/index.html`을 열어 새 카드가 보이는지, 클릭하면
`recall.html`로 이동하는지 확인한다. `http://localhost:8787/sitemap.xml`을 열어 새 줄이
들어갔는지 확인한다.

- [ ] **Step 7: 커밋 및 배포**

```bash
git add index.html sitemap.xml README.md tools/deploy-pages.sh .github/workflows/deploy.yml
git commit -m "feat: 추적검사 계산기를 홈·sitemap·배포 설정에 연결

recall.html/recall-plan.html이 main에 푸시되면 GitHub Actions로 자동 배포되게
배포 파일 목록을 갱신했다.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
git push origin main
```

- [ ] **Step 8: 실배포 확인**

```bash
until curl -s "https://jangbium.github.io/recall.html?v=$RANDOM" | grep -q '추적검사 계산기'; do sleep 15; done
echo "recall.html 배포 확인됨"
curl -s -o /dev/null -w "%{http_code}\n" "https://jangbium.github.io/recall-plan.html?d=2026-09-09&n=3&s=8"
curl -s -o /dev/null -w "%{http_code}\n" "https://jangbium.github.io/js/vendor/qrcode.js"
```
Expected: 세 줄 다 확인 문구/`200`.

브라우저 패널로 실제 배포 주소(`https://jangbium.github.io/recall.html`)에 접속해 Task
6-7에서 한 시나리오를 한 번 더 훑고, 카톡 공유 미리보기가 깨지지 않는지 og 이미지가 뜨는지
확인한다.

---

## Self-Review 메모

- 스펙 3-1·3-2·3-3의 모든 화면 요소(입력 필드, 결과 카드, 하단 바, QR, footer)가 Task
  6·7·8에 반영됨.
- 스펙 4의 URL 스킴은 Task 4의 `parseRecallParams`/`buildRecallQuery`와 정확히 일치(파라미터
  이름 `d/n/s/f/ssl/b` 동일).
- 스펙 5의 우선순위·경계값은 Task 3의 테스트 케이스로 전부 커버(3/4/5/10/11개, 9mm/10mm,
  SSL 밴드, 분할절제 우선, prepInadequate caveat, 절제 없음).
- 스펙 6의 테스트 요구는 Task 2-4에 반영.
- 스펙 7의 QR은 Task 5(벤더링)·Task 7(사용)로 분리해 반영.
- 스펙 8의 문구·법적 요구(진단 아님, 병원 안내 우선, 실명 없음, 출처 표기)는 Task 6·7의
  footer·caveat 문구에 반영.
- 플레이스홀더 없음 — 모든 스텝에 실제 코드/명령이 있다.
